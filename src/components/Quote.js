import React from 'react';
import '../style/Nav.css';

const NavBar = () => (
  <div className="quote-page">
    <p>
      Math is beautiful. But sometimes, this can be hard to see,
      and even harder to convey to students who don’t — yet — share your passion.
    </p>
  </div>
);

export default NavBar;
